reply_user:
  description: >
    You have received a WhatsApp conversation from an unauthorized user.

    {conversation}

    Before replying, call MarkMessageAsReadTool to mark the latest message
    as read and show a typing indicator.

    Send a single, polite message informing the user that they are not
    authorized to use this service.
  expected_output: >
    A single plain-text WhatsApp message, in the same language as the
    conversation, informing the user they are not authorized. Delivered
    via SendTextMessageTool.
  agent: wpp_assistant
